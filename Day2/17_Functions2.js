

// calculate the area of rectangle based on given inputs 

let length = 10;
let width = 3;

let area = 0;

area = length * width;

console.log("area of rectangle is : " + area);


function calculateArea(rectLength, rectWidth){

 let result = rectLength * rectWidth;

    console.log("area of rectangle is : " + result );

}

calculateArea(4, 2);
calculateArea(55, 4);